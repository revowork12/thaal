"use client";import { useState } from "react";import { motion, AnimatePresence } from "framer-motion";import { X, ChevronLeft, ChevronRight } from "lucide-react";const rows = [  {    cells: [      { flex: 6, src: "https://images.unsplash.com/photo-1555244162-803834f70033?w=600&h=900&fit=crop&q=85", alt: "Buffet spread" },      { flex: 4, src: "https://images.unsplash.com/photo-1478145046317-39f10e56b5e9?w=600&h=900&fit=crop&q=85", alt: "Catering setup" },    ],  },  {    cells: [      { flex: 4, src: "https://images.unsplash.com/photo-1466978913421-dad2ebd01d17?w=600&h=900&fit=crop&q=85", alt: "Event coordination" },      { flex: 6, src: "https://images.unsplash.com/photo-1519741497674-611481863552?w=600&h=900&fit=crop&q=85", alt: "Wedding reception" },    ],  },  {    cells: [      { flex: 6, src: "https://images.unsplash.com/photo-1498837167922-ddd27525d352?w=600&h=900&fit=crop&q=85", alt: "Catering spread" },      { flex: 4, src: "https://images.unsplash.com/photo-1540575467063-178a50c2df87?w=600&h=900&fit=crop&q=85", alt: "Corporate event" },    ],  },];const allImages = rows.flatMap((row) => row.cells);const rowVariants = {  hidden: { opacity: 0 },  visible: (i: number) => ({    opacity: 1,    transition: { delay: i * 0.15, duration: 0.5 },  }),};const cellVariants = {  hidden: { opacity: 0, scale: 0.96 },  visible: {    opacity: 1,    scale: 1,    transition: { duration: 0.5, ease: "easeOut" as const },  },};export default function GalleryPreview() {  const [lightboxOpen, setLightboxOpen] = useState(false);  const [currentIndex, setCurrentIndex] = useState(0);  const openLightbox = (index: number) => {    setCurrentIndex(index);    setLightboxOpen(true);    document.body.style.overflow = "hidden";  };  const closeLightbox = () => {    setLightboxOpen(false);    document.body.style.overflow = "";  };  const goNext = () => {    setCurrentIndex((prev) => (prev + 1) % allImages.length);  };  const goPrev = () => {    setCurrentIndex((prev) => (prev - 1 + allImages.length) % allImages.length);  };  return (    <section className="py-20 bg-white overflow-hidden">      <div className="max-w-7xl mx-auto px-6 lg:px-8">        <div className="flex flex-col gap-1 md:gap-2">          {rows.map((row, ri) => (            <motion.div              key={ri}              className="flex flex-row gap-1 md:gap-2 items-start"              custom={ri}              variants={rowVariants}              initial="hidden"              whileInView="visible"              viewport={{ once: true, margin: "-80px" }}            >              {row.cells.map((cell, ci) => {                const globalIndex = rows                  .slice(0, ri)                  .reduce((acc, r) => acc + r.cells.length, 0) + ci;                return (                  <motion.button                    key={globalIndex}                    className="group relative overflow-hidden rounded-lg cursor-pointer min-w-0"                    style={{ flex: `${cell.flex}` }}                    variants={cellVariants}                    onClick={() => openLightbox(globalIndex)}                  >                    <div className="aspect-[9/16] lg:aspect-auto lg:h-[32rem] w-full overflow-hidden">                      <img                        src={cell.src}                        alt={cell.alt}                        className="w-full h-full object-cover transition-transform duration-[800ms] ease-out group-hover:scale-[1.35]"                        loading="lazy"                      />                    </div>                    <div className="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition-all duration-500 flex items-center justify-center">                      <span className="text-white/0 group-hover:text-white/90 text-sm font-medium tracking-wide transition-all duration-500 translate-y-2 group-hover:translate-y-0">                        View Photo                      </span>                    </div>                  </motion.button>                );              })}            </motion.div>          ))}        </div>      </div>      <AnimatePresence>        {lightboxOpen && (          <motion.div            className="fixed inset-0 z-50 bg-black/95 flex items-center justify-center"            initial={{ opacity: 0 }}            animate={{ opacity: 1 }}            exit={{ opacity: 0 }}            onClick={closeLightbox}          >            <button              className="absolute top-6 right-6 text-white/60 hover:text-white transition-colors z-10"              onClick={closeLightbox}              aria-label="Close lightbox"            >              <X size={28} />            </button>            <button              className="absolute left-4 top-1/2 -translate-y-1/2 text-white/60 hover:text-white transition-colors z-10"              onClick={(e) => {                e.stopPropagation();                goPrev();              }}              aria-label="Previous image"            >              <ChevronLeft size={36} />            </button>            <motion.div              key={currentIndex}              className="text-center max-w-[90vw] max-h-[85vh]"              initial={{ opacity: 0, scale: 0.5, y: 40 }}              animate={{ opacity: 1, scale: 1, y: 0 }}              transition={{ duration: 0.5, ease: [0.16, 1, 0.3, 1] }}              onClick={(e) => e.stopPropagation()}            >              <img                src={allImages[currentIndex].src}                alt={allImages[currentIndex].alt}                className="max-w-full max-h-[80vh] object-contain rounded-lg mx-auto"              />              <p className="text-white/60 text-sm mt-4">                {allImages[currentIndex].alt}              </p>            </motion.div>            <button              className="absolute right-4 top-1/2 -translate-y-1/2 text-white/60 hover:text-white transition-colors z-10"              onClick={(e) => {                e.stopPropagation();                goNext();              }}              aria-label="Next image"            >              <ChevronRight size={36} />            </button>            <div className="absolute bottom-6 left-1/2 -translate-x-1/2 text-white/50 text-sm">              {currentIndex + 1} / {allImages.length}            </div>          </motion.div>        )}      </AnimatePresence>    </section>  );}